# Copy this file to `.env` and add your key, or just export the variable.
# Get a key at https://console.anthropic.com  (new accounts get free credit).
# Without a key, the pipeline runs the offline keyword baseline instead.
ANTHROPIC_API_KEY=sk-ant-...
